/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
};

export default nextConfig;

// next.config.mjs
// const nextConfig = {
//   webpack: (config) => {
//     config.resolve.alias["react-toastify"] =
//       "react-toastify/dist/react-toastify.js";
//     return config;
//   },
// };

// export default nextConfig;
